# jen.computer

This project is a port of the previous version of this site using React + TypeScript + Vite.

So far I've moved everything over except for the server-side aspects of the bluesky feed, which includes:
- post retrieval and rendering
- dithered images (which use node buffer)
- some webgl silliness