# jen.computer

This project is a port of my Next.js site using React + TypeScript + Vite.

So far I've moved everything over except for the server-side aspects of the bsky feed, including:
- post retrieval and rendering
- dithered images (which use node buffer)
- some webgl silliness